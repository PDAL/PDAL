where
    An :ref:`expression <PDAL expression>` that limits points passed to a writer
    Points that don't pass the
    expression skip the stage but are available to chained writers in a pipeline.
    [Default: no filtering]

